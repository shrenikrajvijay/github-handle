angular.module("github")
  .controller("handle", function($scope, API_GITHUB, GET_USER, SEARCH_USERS) {

	/*
	*	ngDoc variable
	*  @description holds the variable typed in the input
	*/
	$scope.handle = "";


})